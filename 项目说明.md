# 轻词 LiteWord — 最新项目说明

> 本文档基于当前 `不联网版` 分支的代码实际状态整理，反映了与根目录旧版 `README.md` 的差异。

## 一句话定位

**场景化英语词汇学习 App**。核心设计原则：**学词是为了马上用，学和用之间不该有延迟**——把词汇按真实使用场景组织，学会的词立刻能在对应场景里用上，通过 7 轮间隔重复真正掌握。

当前版本为**纯本地离线版**：数据全部存设备本地、无网络也能学，。剥离了后端、云同步与手机号登录（联网能力以可复用补丁形式保留，见下文）。

---

## 技术栈

- **Capacitor 8**：`@capacitor/core / android / ios / app / preferences / local-notifications`、`@capacitor-community/text-to-speech`、`@ebarooni/capacitor-calendar`
- **纯原生 HTML / CSS / JavaScript**，无前端框架、无构建步骤，源码直接打包进 WebView
- 数据持久化：**Capacitor Preferences**（原生层存储），浏览器环境自动回退 `localStorage` 并做自动迁移
- 目标平台：Android（主，`com.liteword.app`，SDK 24–36）、iOS（已建工程 + CI 云编译，待证书签名）

---

## 场景与词库

内置 8 个使用场景：

| 场景 key | 标签 |
|---|---|
| `ai-prompt` | AI 提示词 |
| `ai-model` | AI 模型 |
| `git` | Git |
| `devops` | 开发部署 |
| `ios` | iOS |
| `software` | 软件 |
| `travel-air` | 航班 |
| `travel-stay` | 酒店 |

关键机制：
- **跨场景词义**：同一词在不同场景可有不同释义（如 `checkout` 在 Git 是「检出分支」、`approach` 在航班是「进近」）。仅在选中对应场景时展示对应释义（`SCENE_WORD_MEANINGS`）。
- **场景化深度搭配**：`COLLOCATIONS / SCENE_EXTRA` 提供每个词在当前场景的高频搭配与额外例句，用于深度记忆面板。
- **自定义词库**：支持直接粘贴 AI 生成的 JSON 导入并合并进 `WORD_BANK`，可删除、可重置进度；导入词缺音标时自动复用内置词库同一词的音标。
- **单词库优先**：一次专注一个词库（单选切换），保证「可学新词数」严格等于选中分类词数。
- 合成词（只在跨场景义里、不在词库本体的词，如 `menu/reset`）不当作可学词；启动时自动清理孤儿记录。

> 注意：当前引导页仅做功能介绍，不再引导选择场景，首次默认专注词库为 `ai-prompt`，场景切换改在「词库管理」面板进行。

---

## 核心功能模块

- **引导页**：全屏磨砂玻璃特色介绍页（「学词，为了马上用」），首次启动展示。
- **首页**：三张统计卡（待复习 / 可学新词 / 已掌握）、「学一组新词」与「开始复习」两个主入口、专注词库进度环、近 30 天学习热力图。
- **学一组新词**：从当前专注词库取新词，一组一组学（组词数可配置，默认 20）。
- **综合复习**：独立入口，复习所有词库已学到的词，展示每个词的场景词义并标注场景标签。
- **词库管理**：查看/切换专注词库、打开词表（按全部/学习中/已掌握筛选）、导入/删除自定义词库、重置进度。
- **深度解析**：单词卡展示词义、音标、发音；移动端可左滑查看当前词在所选场景的高频搭配与例句；桌面端右栏常驻深度记忆面板。
- **设置**：每组词数、每日复习提醒通知、复习日程写入系统日历、口音（美/英）、离线 TTS 引擎、主题、字体、背景视频（换视频/重置）、操作快捷键提示、导入词库、数据备份/恢复、重置全部数据、意见反馈/隐私政策/用户协议。

### 学习机制（SRS）
- 每词 **7 级掌握度**（`currentRound` 1~7，达 7 即「已掌握」）。
- 间隔：首轮立即 → 2 小时 → 1 天 → 2 天 → 7 天 → 15 天；忘记（「还没有记住」）重置回第 1 轮。
- 新词学一次记录一轮；**已掌握词（round 7）遇到新场景义时不下滑**，独立进入 `rec.sceneReview` 轨道走完整 SRS。
- 复习到期词按轮次穿插排列，避免同轮次连续。
- 学习进度按**场景词义**记录（`learnedScenes`），同一词在不同场景可分别记忆。

### 关键交互设计
- **零弹窗续接**：进度静默落盘到 `lw_progress`，首页主按钮在未完成组时变为「继续上次 · 第N/M词」，点击直接续接；中途退出不弹确认框。
- 学习页提供键盘/移动端快捷操作（`Space` 下一个、`A` 上一个、`S` 跳过、`Esc` 退出）；复习到最后一轮可「认识，跳过」。

---

## 主题与外观（重度打磨）

- 三种主题：**浅色** / **深色** / **磨砂**（默认）。磨砂主题用视频背景 + 毛玻璃；浅色主题采用暖色「书香」配色（米色底 `#f0ebe0`、深棕文字），避免刺眼的白与椭圆光圈。
- 两种字体：**经典**（Source Serif 4 斜体用于关键英文元素 + Poppins 正文）与**精选**（全系统无衬线）。
- 单词卡为**自适应设计**（无固定尺寸、JS 锁定释义区高度防抖动），发音时卡片带流动描边光 + 多层柔光共振动效，动效淡出平滑收尾。
- 学习热力图、进度环、数值滚动动画等均做了视觉细化。

---

## 数据存储（Capacitor Preferences keys）

| key | 内容 |
|---|---|
| `lw_config` | 用户配置（组词数、口音、主题、字体、背景视频、专注词库、提醒等） |
| `lw_records` | 学习记录（每词 `currentRound`、`learnedScenes`、`sceneReview` 等） |
| `lw_onboard` | 引导完成状态 |
| `lw_streak` | 连续学习天数 |
| `lw_progress` | 学习进度存档（零弹窗续接） |
| `lw_custom_words` | 导入的自定义词库数据 |
| `lw_custom_cats` | 自定义词库分类标签 |
| `lw_calendar_events` | 已写入的复习日历事件记录 |
| `lw_schema_version` | 数据契约版本号（多设备同步迁移锚点，目前 v1） |

`store.js` 还实现了 schema 版本号接口 + 逐级迁移注册表（`MIGRATIONS`），为将来多设备同步预留结构演进的安全网。

---

## 目录结构

```
d:\开发\danci
├── www/                        # 前端唯一源码目录
│   ├── index.html              # 页面结构（引导/首页/学习/完成/设置等 + 各面板）
│   ├── style.css               # 全部样式（三主题、玻璃卡、卡片共振、响应式）
│   ├── app.js                  # 核心逻辑（渲染、学习/复习、词库、设置、主题、日历）
│   ├── words.js                # 词库数据（WORD_BANK、SCENE_LABELS、跨场景义、深度搭配）
│   ├── store.js                # 持久化层（Preferences/localStorage + schema 迁移）
│   ├── fonts/                  # 字体资源
│   ├── bg.mp4                  # 默认背景视频（磨砂主题）
│   └── test-inject.html        # 测试辅助页
├── android/                    # Android 工程（Capacitor 生成）
├── ios/                        # iOS 工程 + 上架说明（README-iOS.md）
├── scripts/install_to_phone.ps1 # 一键构建+安装到手机（cap copy + gradle + adb）
├── patches/online/             # 联网版可复用补丁（后端 FastAPI + sync.js + apply.ps1）
├── .github/workflows/build-ios.yml # iOS 云编译 CI（macOS，当前无签名验证构建）
├── capacitor.config.json       # Capacitor 配置（appId: com.liteword.app）
├── danci-latest.apk            # 最新 APK（releases/ 存历史归档）
└── 项目说明.md                  # 本文档
```

---

## 版本策略与分支

三条核心分支：
- `master`（联网版）：保留云同步、登录与 FastAPI 后端。
- `不联网版`（当前主要开发分支）：纯本地存储，已移除后端与同步模块。
- `gh-pages`：`www/` 的浏览器预览，用于 iPhone 浏览器测试核心功能。

**联网能力补丁化**：`patches/online/` 内含 `backend/`（FastAPI 认证 + 同步）、`www/sync.js`、幂等脚本 `apply.ps1` 与 `README.md`，可一键把联网功能合入离线版。

---

## 构建

1. 改 `www/` 源码 → 根目录 `npx cap sync android`（或脚本内 `cap copy`）同步到 Android assets。
2. `android/gradlew assembleDebug|assembleRelease`。
3. 产物在重定向构建目录 `C:/trae_build/danci/app-build/outputs/...`，复制回根目录按规范命名；历史版本归档进 `releases/`。
4. **自动装机**：`scripts/install_to_phone.ps1` 依次做 `adb reverse` 端口转发、`cap copy`、构建、`adb install -r` 覆盖安装（保留数据）并自动启动。

构建环境要点（记录在案）：构建目录重定向到纯 ASCII 路径规避中文路径 Gradle VFS 报错；Gradle 缓存指向 `D:/gradle-cache`、npm 缓存指向 `D:/npm-cache`；`gradle.properties` 已调优（并行 + 缓存 + workers=8）使 debug 增量构建约 15s。

---

## 已知待办 / 提醒

- 当前「不联网版」已去掉登录与云同步，升级/换机需用「导出/恢复备份」手动迁移。
- iOS 工程已可用 CI 验证可构建性（无签名），真机安装与上架需配置 Apple 证书（见 `ios/README-iOS.md`）。
- 如需重新启用联网能力，运行 `patches/online/apply.ps1` 并参考其 README。